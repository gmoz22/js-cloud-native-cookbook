{
  "name": "cncb-create-stack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "sls package --verbose -r us-east-1 --stage test",
    "dp:lcl": "sls deploy --verbose -r us-east-1",
    "rm:lcl": "sls remove --verbose -r us-east-1"
  },
  "devDependencies": {
    "serverless": "^3.38.0"
  }
}