{
  "name": "cncb-event-stream",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "sls package -r us-east-1 -s test",
    "dp:lcl": "sls deploy --verbose -r us-east-1",
    "rm:lcl": "sls remove -r us-east-1"
  },
  "devDependencies": {
    "serverless": "^3.38.0"
  },
  "dependencies": {
    "@aws-sdk/client-kinesis": "^3.817.0",
    "uuid": "^11.1.0"
  }
}
